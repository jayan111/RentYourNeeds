# URL of your deployed backend — include /api, no trailing slash
NEXT_PUBLIC_API_URL=https://your-backend.railway.app/api

# Stripe publishable key (safe to expose — it's public-facing)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_xxx