services:
  - type: web
    name: rentyourneeds-admin
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
      # Set NEXT_PUBLIC_API_URL in the Render dashboard
